% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance_metrics.R
\name{loess_smooth_series}
\alias{loess_smooth_series}
\title{Smooth a performance series using LOESS}
\usage{
loess_smooth_series(x, y, xloess, span = 0.75, trim = c(0.02, 0.98))
}
\arguments{
\item{x}{Numeric vector. Covariate values (e.g. observed missingness
proportions).}

\item{y}{Numeric vector. Response values to be smoothed (e.g. performance
metrics).}

\item{xloess}{Numeric vector. Grid of covariate values at which the smoothed
curve is evaluated.}

\item{span}{Numeric. LOESS smoothing parameter. Defaults to 0.75.}

\item{trim}{Numeric vector of length 2. Lower and upper quantiles used to
trim extreme values of \code{x} before fitting. Defaults to
\code{c(0.02, 0.98)}.}
}
\value{
Numeric vector of smoothed values evaluated at \code{xloess}.
}
\description{
Fits a locally weighted regression (LOESS) of a response variable on a
covariate and predicts the smoothed values on a specified grid. Extreme
covariate values can be trimmed prior to smoothing to reduce boundary
effects.
}
\details{
Missing values in \code{x} or \code{y} are removed prior to fitting.
The LOESS fit uses a local linear model (\code{degree = 1}).
}
